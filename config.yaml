DbPath: /mnt/c/Users/Adam/Documents/C++_Projects/ZestDB/DB # Path to the folder containing db files
SegSize: 128000 # Max data segment size
MaxKeySize: 100000 # Max key length
MaxValueSize: 100000 # Max value length
CacheSize: 1000 # Max number of values in the cache
CompactingInterval: 10 # The ammount of time in seconds between each compaction of the INDEX file
KeyValidation: ".*" # Regex for keys
ValueValidation: ".*" # Regex for values

Port: 7321
NetworkValidation: ".*" # Regex for allowed incoming request IP addresses

isDebug: false # Degug mode for the logger

users:
  - user: bob
    password: bob